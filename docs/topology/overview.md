# Network Topology Overview

- `router-garage`: gateway role host.
- `router-office`: access point role host.
- VLAN and SSID mappings are defined in `inventory/network.yaml`.
- Service controls (Tailscale, AdGuard, WireGuard policy) are under `services.*` in `inventory/network.yaml`.

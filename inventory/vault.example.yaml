---
# Placeholder secrets template.
# Copy to inventory/vault.yaml, then edit and encrypt with Ansible Vault.

# Gateway WAN (required when network.gateway_router.test_mode.enabled=false)
vault_gateway_router_wan_pppoe_username: "REPLACE_WITH_ISP_USERNAME"
vault_gateway_router_wan_pppoe_password: "REPLACE_WITH_ISP_PASSWORD"

# Gateway test-mode repeater uplink (required when network.gateway_router.test_mode.enabled=true)
vault_gateway_router_test_repeater_ssid: "REPLACE_WITH_UPSTREAM_WIFI_SSID"
vault_gateway_router_test_repeater_password: "REPLACE_WITH_UPSTREAM_WIFI_PASSWORD"


# Gateway management Wi-Fi
vault_gateway_router_mgmt_password: "REPLACE_WITH_GATEWAY_MGMT_WIFI_PASSWORD"

# AP-repeater Wi-Fi PSKs
vault_wifi_key_manor: "REPLACE_WITH_MANOR_WIFI_KEY"
vault_wifi_key_iot: "REPLACE_WITH_IOT_WIFI_KEY"
vault_wifi_key_guest: "REPLACE_WITH_GUEST_WIFI_KEY"

# Tailscale auth
# Option A: auth key
# tailscale_auth_key: ""
# Option B: OAuth credentials
vault_tailscale_oauth_client_id: ""
vault_tailscale_oauth_client_secret: ""

# Optional WireGuard policy routing secrets
vault_wireguard_private_key_london: ""
vault_wireguard_public_key_london: ""
vault_wireguard_preshared_key_london: ""
vault_wireguard_private_key_amsterdam: ""
vault_wireguard_public_key_amsterdam: ""
vault_wireguard_preshared_key_amsterdam: ""

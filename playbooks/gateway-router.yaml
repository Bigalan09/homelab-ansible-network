---
- name: Configure gateway router baseline and services
  hosts: gateway
  become: false
  gather_facts: false
  vars_files:
    - ../inventory/network.yaml
    - ../inventory/vault.yaml
  roles:
    - router_garage
    - tailscale_openwrt
    - adguard_home_openwrt
    - wireguard_policy_openwrt

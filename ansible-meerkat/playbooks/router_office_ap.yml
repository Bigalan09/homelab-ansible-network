---
- name: Configure office router/AP (access point baseline)
  hosts: access_points
  become: false
  gather_facts: false

  vars_files:
    - ../group_vars/all/network.yml
    - ../group_vars/all/vault.yml

  roles:
    - role: router_office_ap

---
- name: Configure garage router core network baseline
  hosts: gateway
  become: false
  gather_facts: false

  vars_files:
    - ../../group_vars/all/network.yml
    - ../../group_vars/all/vault.yml

  roles:
    - role: router_garage

---
- name: Configure garage router AdGuard Home DNS profile
  hosts: gateway
  become: false
  gather_facts: false

  vars_files:
    - ../../group_vars/all/network.yml
    - ../../group_vars/all/vault.yml

  roles:
    - role: adguard_home_openwrt
